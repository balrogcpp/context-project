diff --git a/CMakeLists.txt b/CMakeLists.txt
index a2fcacc..f757760 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -2266,7 +2266,7 @@ elseif(APPLE)
           int main() { return 0; }" HAVE_FRAMEWORK_COREHAPTICS)
       if(HAVE_FRAMEWORK_GAMECONTROLLER AND HAVE_FRAMEWORK_COREHAPTICS)
         # Only enable MFI if we also have CoreHaptics to ensure rumble works
-        set(SDL_JOYSTICK_MFI 1)
+        #set(SDL_JOYSTICK_MFI 1)
         set(SDL_FRAMEWORK_GAMECONTROLLER 1)
         set(SDL_FRAMEWORK_COREHAPTICS 1)
       endif()
diff --git a/src/joystick/apple/SDL_mfijoystick.m b/src/joystick/apple/SDL_mfijoystick.m
index 48e9051..cd8a436 100644
--- a/src/joystick/apple/SDL_mfijoystick.m
+++ b/src/joystick/apple/SDL_mfijoystick.m
@@ -763,7 +763,7 @@ static bool IOS_JoystickInit(void)
     }

 #ifdef SDL_PLATFORM_MACOS
-    if (@available(macOS 11.0, *)) {
+    if (floor(NSAppKitVersionNumber) > NSAppKitVersionNumber11_0) {
         // Continue with initialization on macOS 11+
     } else {
         return true;
@@ -1769,7 +1769,7 @@ bool IOS_SupportedHIDDevice(IOHIDDeviceRef device)
         return false;
     }

-    if (@available(macOS 11.0, *)) {
+    if (floor(NSAppKitVersionNumber) > NSAppKitVersionNumber11_0) {
         const int MAX_ATTEMPTS = 3;
         for (int attempt = 0; attempt < MAX_ATTEMPTS; ++attempt) {
             if ([GCController supportsHIDDevice:device]) {
diff --git a/src/video/cocoa/SDL_cocoaevents.m b/src/video/cocoa/SDL_cocoaevents.m
index fd178b8..ef30c57 100644
--- a/src/video/cocoa/SDL_cocoaevents.m
+++ b/src/video/cocoa/SDL_cocoaevents.m
@@ -326,7 +326,7 @@ static void Cocoa_DispatchEvent(NSEvent *theEvent)
        behaviour there.  https://github.com/libsdl-org/SDL/issues/10340
        (13.6 still needs it, presumably 13.7 does, too.) */
     bool background_app_default = false;
-    if (@available(macOS 14.0, *)) {
+    if (floor(NSAppKitVersionNumber) >= NSAppKitVersionNumber14_0) {
         background_app_default = true;  /* by default, don't explicitly activate the dock and then us again to force to foreground */
     }
 
