version: "3.3"
services:
  sample-linux:
    build:
      context: .
      dockerfile: ./docker/sample/linux.Dockerfile
      args:
        GIT_HASH: ${GIT_HASH}
    container_name: sample-linux
    volumes:
      - .:/var/artifacts
    command: cp -R /mnt/build/artifacts /var/artifacts
  sample-windows:
    build:
      context: .
      dockerfile: ./docker/sample/windows.Dockerfile
      args:
        GIT_HASH: ${GIT_HASH}
    container_name: sample-windows
    volumes:
      - .:/var/artifacts
    command: cp -R /mnt/build/artifacts /var/artifacts
  sample-apple:
    build:
      context: .
      dockerfile: ./docker/sample/apple.Dockerfile
      args:
        GIT_HASH: ${GIT_HASH}
    container_name: sample-apple
    volumes:
      - .:/var/artifacts
    command: cp -R /mnt/build/artifacts /var/artifacts
  sample-android:
    build:
      context: .
      dockerfile: ./docker/sample/android.Dockerfile
      args:
        GIT_HASH: ${GIT_HASH}
    container_name: sample-android
    volumes:
      - .:/var/artifacts
    command: cp -R /mnt/build/artifacts /var/artifacts
