image: docker:git

services:
  - docker:dind

variables:
  GIT_SUBMODULE_STRATEGY: recursive
  GIT_DEPTH: "1"

build:
  stage: compile
  script:
    - sh docker_compile.sh
  artifacts:
    paths:
      - "artifacts/*"
